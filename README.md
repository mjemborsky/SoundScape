# SoundScape


Summary

This is an app in development for Windows and Linux OS that allows a user to have complete control over their various audio sources. The app automatically determines and isolates each audio source (by app), places them in a virtual "room", and then allows the user to place it wherever they would like, changing audio properties such as panning, volume, stereo, reverb, and possible other effects.
